import { createTheme } from '@suid/material';

export const darkTheme = createTheme({
    palette: {
        primary: {
            main: '#1976d2',
            contrastText: 'white',
        },
    },
});
