import { createTheme } from '@suid/material';

export const lightTheme = createTheme({
    palette: {
        primary: {
            main: '#1976d2',
            contrastText: 'white',
        },
    },
});
